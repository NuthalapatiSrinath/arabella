{
    "buildCommand": "cd arabella-frontendd-main && npm install && npm run build",
    "outputDirectory": "arabella-frontendd-main/build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}
